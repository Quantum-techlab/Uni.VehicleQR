rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for all authenticated users.
    // This is required for features like passport photo uploads.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
