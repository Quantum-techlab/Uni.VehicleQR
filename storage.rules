rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access to allow CORS preflight requests to succeed.
      allow read;
      // Restrict write access to only authenticated users.
      allow write: if request.auth != null;
    }
  }
}
